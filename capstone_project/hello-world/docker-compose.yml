version: "1.0"  
services:
  ansible:
    build: 
      context: ./ansible_base
      dockerfile: Dockerfile
    container_name: ansible_controller_project
    volumes:
      - type: bind
        source: ./ansible_files
        target: /home/ansible_controller_project/ansible_files
  ansible_capstone_project_a:
    build: 
      context: ./ansible_base
      dockerfile: Dockerfile
    container_name: ansible_capstone_project_a
