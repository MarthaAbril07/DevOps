---
name: Pipeline for capstone project
on:
  push:
    branches:
      - testworkflows
  pull_request:
      branches:
      - testworkflows
jobs:
    firstjob:
       name: Pipeline for capstone project
       runs-on: ubuntu-latest
       steps:
        - name: Verificar ruta y contenido
          run:  pwd
        - name: Prueba carpeta
          run: ls
        - name: Imprimir directorio actual1
          run: pwd
        - name: Running tests
          run: npm install && npm test && docker compose up
